apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: client
  labels:
    app: client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: client
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
        - image: gcr.io/tcc-ufrj/client:v5
          name: client
          imagePullPolicy: Always
          ports:
          - containerPort: 80
            name: client
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: client
  name: client
spec:
  type: LoadBalancer
  loadBalancerIP: 35.198.46.103
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: client